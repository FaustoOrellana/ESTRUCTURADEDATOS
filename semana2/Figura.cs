public abstract class Figura
{
    // Método abstracto para calcular el área que será implementado por cada figura
    public abstract double CalcularArea();

    // Método abstracto para calcular el perímetro que será implementado por cada figura
    public abstract double CalcularPerimetro();
}